<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results - YouTube Clone</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #f7f7f7;
            color: #333;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            padding: 30px 0;
        }

        h1 {
            margin-top: 50px;
            font-size: 36px;
            color: #111;
            font-weight: 700;
            margin-bottom: 30px;
        }

        /* Search Box */
        .search-container {
            display: flex;
            justify-content: center;
            width: 100%;
            max-width: 900px;
            margin-bottom: 40px;
            padding: 10px;
            border-radius: 50px;
            background-color: #fff;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        .search-container input {
            flex-grow: 1;
            padding: 12px 20px;
            font-size: 18px;
            border: 2px solid #ccc;
            border-radius: 50px;
            outline: none;
            transition: all 0.3s ease-in-out;
        }

        .search-container input:focus {
            border-color: #0066cc;
            box-shadow: 0 0 5px rgba(0, 102, 204, 0.3);
        }

        .search-container button {
            padding: 12px 24px;
            background-color: #0066cc;
            color: #fff;
            border: none;
            border-radius: 30px;
            cursor: pointer;
            font-size: 16px;
            margin-left: 10px;
        }

        .search-container button:hover {
            background-color: #005bb5;
        }

        /* Video Grid */
        .video-grid {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
            width: 100%;
        }

        .video-card {
            width: calc(33.33% - 20px);
            background-color: #fff;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .video-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
        }

        .video-card img {
            width: 100%;
            height: auto;
        }

        .video-card .content {
            padding: 15px;
        }

        .video-card h3 {
            font-size: 18px;
            color: #333;
            margin-bottom: 10px;
        }

        .video-card p {
            color: #777;
            font-size: 14px;
            margin-bottom: 0;
        }

        /* Media Queries */
        @media screen and (max-width: 768px) {
            .video-card {
                width: calc(50% - 20px);
            }
        }

        @media screen and (max-width: 480px) {
            .video-card {
                width: 100%;
            }
        }
        .logo {
    position: absolute;
    top: 20px;
    left: 20px;
}

.logo a {
    font-size: 24px;
    font-weight: bold;
    color: #FF0000; /* YouTube-like red color */
    text-decoration: none;
}

.logo a:hover {
    color: #cc0000; /* Slightly darker red on hover */
}
    </style>
</head>
<body>
    <div class="logo">
        <a href="/home" style="text-decoration: none; color: #FF0000; font-weight: bold; font-size: 24px; display: flex; align-items: center;">
            <i class="fa-brands fa-youtube" style="color: #e40c0c; font-size: 48px; margin-right: 8px;"></i>
            YouTube Clone
        </a>
    </div>

    <h1>Search Results for "<%= query %>"</h1>

    <!-- Video Grid -->
    <div class="video-grid">
        <% results.forEach(video => { %>
            <div class="video-card">
                <a href="/video/<%= video.id.videoId %>">
                    <img src="<%= video.snippet.thumbnails.high.url %>" alt="<%= video.snippet.title %>">
                    <div class="content">
                        <h3><%= video.snippet.title %></h3>
                        <p><%= video.snippet.channelTitle %></p>
                    </div>
                </a>
            </div>
        <% }) %>
    </div>
</body>
</html>
